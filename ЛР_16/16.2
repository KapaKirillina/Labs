#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int N, A, D, d = 1, i;
	printf("Введите целое число N (N>1), первый член A и знаменатель D\n");
	scanf_s("%d %d %d", &N, &A, &D);
	int* a = new int[N];
	for (i = 0; i < N; i++) {
		a[i] = A * d;
		d *= D;
	}
	for (i = 0; i < N; i++) {
		printf("%d ", a[i]);
	}
	return 0;
}
