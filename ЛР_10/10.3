#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int x;
	printf("Введите целое положительное число\n");
	scanf_s("%d", &x);
	int c = x % 2;
	int k = 0;
	while (x > 0) {
		x /= 10;
		k++;
	}
	if ((c == 0) && (k == 2))
		printf("true");
	else
		printf("false");
}
