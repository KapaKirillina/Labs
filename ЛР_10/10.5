#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int x;
	printf("Введите четырехзначное число\n");
	scanf_s("%d", &x);
	int a, b, c, d;
	a = x / 1000;
	b = (x / 100) % 10;
	c = (x / 10) % 10;
	d = x % 10;
	if ((a == d) && (b == c))
		printf("true");
	else
		printf("false");
}
