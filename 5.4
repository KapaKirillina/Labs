#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int x1, x2, y1, y2;
	printf("Введите координаты первой вершины\n");
	scanf_s("%d %d", &x1, &y1);
	printf("Введите координаты второй вершины\n");
	scanf_s("%d %d", &x2, &y2);
	int S, P, x, y;
	x = abs(x1 - x2);
	y = abs(y1 - y2);
	S = x * y;
	P = 2 * (x + y);
	printf("Периметр равен %d, площадь равна %d", P, S);
	return 0;
}
