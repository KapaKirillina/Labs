#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	float a, b;
	float sum, r, p, d;
	printf("Введите два  ненулевыхчисла через пробел \n");
	scanf_s("%f %f", &a, &b);
	sum = a * a + b * b;
	r = a * a - b * b;
	p = (a * a) * (b * b);
	d = (a * a) / (b * b);
	printf("Сумма их квадратов равна %f \n", sum);
	printf("Разность их квадратов равна %f \n", r);
	printf("Произведение их квадратов равна %f \n", p);
	printf("Частное их квадратов равна %f \n", d);
	return 0;
}
