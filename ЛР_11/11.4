#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	float x, y;
	printf("Введите координаты точки\n");
	scanf_s("%f %f", &x, &y);
	if ((x > 0) && (y > 0))
		printf("Данная точка находится в первой четверти");
	else if ((x < 0) && (y > 0))
		printf("Данная точка находится во второй четверти");
	else if ((x < 0) && (y < 0))
		printf("Данная точка находится в третьей четверти");
	else if ((x > 0) && (y < 0))
		printf("Данная точка находится в четвертой четверти");
}
