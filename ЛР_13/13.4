#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int N;
	float sum = 0;
	float A;
	printf("Введите целое и веществнное число\n");
	scanf_s("%d %f", &N, &A);
	for (int i = 0; i <= N; i++) {
		sum += pow(A, i);
	}
	printf("%f", sum);
	return 0;
}
