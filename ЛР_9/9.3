#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int K, N;
	printf("Введите целое число K, лежащее в диапазоне 1-365\n");
	scanf_s("%d", &K);
	printf("Введите целое число N, лежащее в диапазоне 1-7\n"); 
	scanf_s("%d", &N);
	int res = (K + N - 2) % 7 + 1;
	printf("Номер дня недели для %d-го дня года, если известно, что в этом году 1 января было днем недели с номером %d, равен %d", K, N, res);
	return 0;
}
