#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int x;
	printf("Введите трехзначное число\n");
	scanf_s("%d", &x);
	int a, b, c;
	a = x / 100;
	b = (x / 10) % 10;
	c = x % 10;
	if ((a < b) && (b < c) || (c < b) && (b < a))
		printf("true");
	else
		printf("false");
}
