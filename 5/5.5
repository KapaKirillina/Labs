#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int x1, x2, x3, y1, y2, y3;
	printf("Введите координаты первой вершины\n");
	scanf_s("%d %d", &x1, &y1);
	printf("Введите координаты второй вершины\n");
	scanf_s("%d %d", &x2, &y2);
	printf("Введите координаты третьей вершины\n");
	scanf_s("%d %d", &x3, &y3);
	double S, P, a, b, c;
	a = sqrt(pow((x1 - x3), 2) + pow((y1 - y3), 2));
	b= sqrt(pow((x1 - x2), 2) + pow((y1 - y2), 2));
	c= sqrt(pow((x2 - x3), 2) + pow((y2 - y3), 2));
	P = a + b + c;
	double p = P / 2;
	S = sqrt(p * (p - a) * (p - b) * (p - c));
	printf("Периметр равен %f, площадь равна %f", P, S);
	return 0;
}
