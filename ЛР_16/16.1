#include <stdio.h>
#include <stdlib.h>
int main()
{
	system("chcp 1251");
	system("cls");
	int N;
	printf("Введите целое число N (N>0)\n");
	scanf_s("%d", &N);
	for (int i = 1; i <= 2*N; i += 2) {
		printf("%d ", i);
	}
	return 0;
}
